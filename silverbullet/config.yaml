name: "SilverBullet Knowledge System"
version: "1.0.0"
slug: "silverbullet"
description: "Self-hosted SilverBullet Add-on"
arch:
  - aarch64
  - armv7
  - amd64
startup: application
boot: auto
host_network: false

ports:
  3001/tcp: 3001

map:
  - config:rw

options:
  space_path: "/config/silverbullet-space"

schema:
  space_path: str
